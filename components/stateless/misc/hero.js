import { Box, Typography } from "@mui/material";

const Hero = () => {
  return (
    <Box p={3}>
      <Typography variant="h4">Vinson</Typography>
      <Typography variant="h6">Software Developer</Typography>
    </Box>
  );
};

export default Hero;
